<%inherit file="base.mako" />

<%def name="title()">Internal Server Error</%def>

	<blockquote>
		<h3><strong>Our servers are having a headache.</strong></h3>
		<p>The server encountered an internal error or misconfiguration (they don't like human randomness that much).</p>
		<p>You can try refreshing in a couple of seconds. If you find this same page again, <a href="javascript:Popego.Feedback.showForm();">contact us</a> and we'll try to help you.</p>
	</blockquote>

