from twisted.application import internet, service
from jq.consumer.server import JobConsumerFactory

def attachConsumerService(port, app):
    service =  internet.TCPServer(port, JobConsumerFactory())
    service.setServiceParent(app)

application = service.Application("JobConsumer Application")
